namespace utilities.ui
{
    public enum TransitionType
    {
        None,
        Fade,
        Slide,
        Scale,
        Custom
    }

    public enum DocumentState
    {
        Hidden,
        Visible,
        Transitioning
    }

    public enum DocBehavior
    {
        single,
        multiple
    }
}
