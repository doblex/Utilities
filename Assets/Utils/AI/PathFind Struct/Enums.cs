namespace utilities.AI
{
    public enum AIState
    {
        Idle,
        Moving,
        Dead
    }

    public enum AStarAgentStatus
    {
        Invalid,
        InProgress,
        Finished,
        RePath
    }
}
